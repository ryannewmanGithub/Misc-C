#include <math.h>
#include <stdio.h>

int main() {
    //int t = pow(5, 2);
    //printf("%d\n", t);
    printf("\\t");
    printf("\n");
    

    return 0;
    int c;

    c = getchar();

    while (c != EOF) {
        putchar(c);
        c = getchar();
    }
}